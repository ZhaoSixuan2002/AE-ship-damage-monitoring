# 工作区清理方案

## 🎯 清理目标

在脚本重组完成后,以下文件和目录可以安全删除,**不会影响重组后的主流程运行**。

---

## ✅ 可以安全删除的文件

### 0. 主控脚本（已废弃）⭐ 新增

```
main.py                       ← 旧的主控脚本,依赖已废弃的config_runtime.py
```

**原因**: 
- 依赖 `config_runtime.py`（本身就要删除）
- 调用旧脚本名称（`batch_controller.py` 等,都要删除）
- 设计理念已改变：从"集中式控制"改为"直接执行需要的流程"
- 新方式更简单：直接运行 `python script/XX_*_main.py` 即可

**新的执行方式**:
```bash
# 旧方式（需要main.py + config_runtime.py）
python main.py  # ❌ 不再需要

# 新方式（直接执行需要的流程）
python script/01_generate_training_data_main.py  # ✅ 简单直接
python script/02_inp_to_vtu_main.py
python script/03_preprocess_training_data_main.py
# ... 按需执行
```

### 1. 旧配置文件（已废弃）

```
script/config.py              ← 集中配置已废弃,参数已分散到各脚本顶部
script/config_runtime.py      ← 运行时配置已废弃,参数已内嵌到各脚本
```

**原因**: 
- 所有 `*_main.py` 脚本已不再依赖这两个配置文件
- 参数按自然逻辑顺序直接写在各脚本顶部
- 符合"参数本地化"原则

**确认方法**:
```bash
# 检查是否还有依赖（应返回空）
grep -r "from.*config import\|import.*config" script/*_main.py
grep -r "from.*config import\|import.*config" script/*_auxiliary.py
grep -r "from.*config import\|import.*config" script/*_helper.py
```

---

### 2. 旧工具模块（已废弃）

```
script/utils/                 ← 整个目录可删除
├── plotting.py              ← 绘图函数已内嵌到各脚本
├── transforms.py            ← 数据变换已内嵌到各脚本
├── __init__.py
└── __pycache__/
```

**原因**:
- 重构后采用"代码直接内嵌"原则
- 每个脚本自包含所需的工具函数
- 避免跨文件依赖,提高可维护性

**确认方法**:
```bash
# 检查是否还有依赖（应返回空）
grep -r "from utils\|import utils" script/*_main.py
grep -r "from utils\|import utils" script/*_auxiliary.py
grep -r "from utils\|import utils" script/*_helper.py
```

---

### 3. 旧流程脚本（已重构）

```
script/batch_controller.py        ← 已重构为 01_generate_training_data_main.py
script/single_run.py               ← 已整合到 01_async_postprocess_auxiliary.py
script/postprocess_async.py        ← 已整合到 01_async_postprocess_auxiliary.py

script/inp_to_vtu_with_elsets.py   ← 已重构为 02_inp_to_vtu_main.py

script/preprocess_data.py          ← 已重构为 03_preprocess_training_data_main.py

script/train_model.py              ← 已重构为 04_train_model_main.py

script/eval_residuals_by_dimension.py  ← 已重构为 05_determine_thresholds_main.py

script/preprocess_validate.py      ← 已重构为 07_preprocess_validation_data_main.py

script/damage_validation.py        ← 已重构为 08_damage_validation_main.py

script/time_history_animation.py   ← 已重构为 09_render_time_history_main.py

script/render_vtu_animation.py     ← 已重构为 10_render_vtu_animation_main.py

script/view_damage_animation_interactive.py  ← 已重构为 10_interactive_vtu_viewer_helper.py
```

**原因**:
- 每个旧脚本都有对应的重构版本
- 新脚本功能完整,且遵循统一的命名规范
- 旧脚本仅用于向后兼容,实际已不需要

**确认方法**:
- 检查 `main.py` 是否还在调用这些旧脚本（当前仍在调用）
- 需要先更新 `main.py` 改为调用新脚本

---

### 4. 无用脚本

```
script/generate_animation_only.py    ← 功能已整合到其他流程
script/analyze_crack_results.py      ← 临时分析脚本,不再需要
```

**原因**:
- 这些脚本在重组方案中已明确标记为"待删除"
- 功能已被新流程覆盖或不再需要

---

### 5. 旧输出文件（可选清理）

```
script/whole_from_inp.vtu         ← 已移到 02_inp_to_vtu_output/
script/measures_ID_auto.csv       ← 已移到 02_inp_to_vtu_output/
script/element_sets_summary.txt   ← 已移到 02_inp_to_vtu_output/
script/element_groups.inp         ← 中间文件,可删除
```

**原因**:
- 这些文件是旧流程直接输出到 `script/` 目录的
- 新流程已将输出规范化到独立的 `_output/` 目录
- 保留在 `script/` 根目录会造成混乱

---

### 6. Python缓存目录

```
script/__pycache__/               ← Python字节码缓存,可随时重建
script/.vs/                       ← Visual Studio缓存,可删除
```

**原因**:
- 自动生成的缓存文件
- 删除后运行时会自动重建
- 不影响任何功能

---

## ⚠️ 不需要修改任何文件

### main.py（直接删除，无需更新）✅

**原因**: 
- `main.py` 本身依赖要删除的 `config_runtime.py`
- 调用的都是要删除的旧脚本
- 新的设计理念不需要主控脚本
- 直接执行需要的 `XX_*_main.py` 更简单清晰

**旧的执行方式**:
```bash
# 1. 先修改 config_runtime.py 设置开关
ENABLE_DATA_GENERATION = 1
ENABLE_PREPROCESSING = 1
# ...

# 2. 运行 main.py
python main.py
```

**新的执行方式**（更简单）:
```bash
# 直接按需执行需要的流程，无需任何配置文件
cd script

# 按顺序执行（或按需跳过某些步骤）
python 01_generate_training_data_main.py
python 02_inp_to_vtu_main.py
python 03_preprocess_training_data_main.py
python 04_train_model_main.py
python 05_determine_thresholds_main.py
python 06_generate_validation_data_main.py
python 07_preprocess_validation_data_main.py
python 08_damage_validation_main.py
python 09_render_time_history_main.py
python 10_render_vtu_animation_main.py
```

**一键运行所有流程**（如果需要）:
```bash
# PowerShell
cd script
Get-ChildItem -Filter "*_main.py" | Sort-Object Name | ForEach-Object { python $_.Name }

# Bash
cd script
for script in *_main.py; do python "$script"; done
```

---

## 📋 清理步骤建议

### 阶段1: 验证（推荐先做）

```bash
# 1. 测试新流程是否完全正常工作
cd c:\data\AE-main\script

# 测试每个主流程（使用小数据量）
python 01_generate_training_data_main.py  # 修改参数：NUM_ITERATIONS = 10
python 02_inp_to_vtu_main.py
python 03_preprocess_training_data_main.py  # 修改参数：PREPROCESS_MAX_FOLDERS = 10
python 04_train_model_main.py  # 修改参数：AE_EPOCHS = 100
python 05_determine_thresholds_main.py
python 06_generate_validation_data_main.py  # 修改参数：NUM_ITERATIONS = 10
python 07_preprocess_validation_data_main.py
python 08_damage_validation_main.py
python 09_render_time_history_main.py
python 10_render_vtu_animation_main.py

# 2. 确认所有流程都能正常运行
```

### 阶段2: 清理旧文件

**选项A: 安全备份后删除（推荐）**

```bash
# 1. 创建备份目录
mkdir -p archive/old_scripts
mkdir -p archive/old_configs
mkdir -p archive/old_utils

# 2. 移动（而不是删除）旧文件
cd c:\data\AE-main

# 移动主控脚本
mv main.py archive/old_scripts/

# 移动旧配置
cd script
mv config.py ../archive/old_configs/
mv config_runtime.py ../archive/old_configs/

# 移动旧工具模块
mv utils ../archive/old_utils/

# 移动旧流程脚本
mv batch_controller.py ../archive/old_scripts/
mv single_run.py ../archive/old_scripts/
mv postprocess_async.py ../archive/old_scripts/
mv inp_to_vtu_with_elsets.py ../archive/old_scripts/
mv preprocess_data.py ../archive/old_scripts/
mv train_model.py ../archive/old_scripts/
mv eval_residuals_by_dimension.py ../archive/old_scripts/
mv preprocess_validate.py ../archive/old_scripts/
mv damage_validation.py ../archive/old_scripts/
mv time_history_animation.py ../archive/old_scripts/
mv render_vtu_animation.py ../archive/old_scripts/
mv view_damage_animation_interactive.py ../archive/old_scripts/

# 移动无用脚本
mv generate_animation_only.py ../archive/old_scripts/
mv analyze_crack_results.py ../archive/old_scripts/

# 移动旧输出文件
mv whole_from_inp.vtu ../archive/
mv element_groups.inp ../archive/

# 删除缓存（可安全删除,无需备份）
rm -rf __pycache__
rm -rf .vs
```

**选项B: 直接删除（谨慎）**

```bash
cd c:\data\AE-main

# 删除主控脚本
rm main.py

# 删除旧配置
cd script
rm config.py
rm config_runtime.py

# 删除旧工具模块
rm -rf utils

# 删除旧流程脚本
rm batch_controller.py
rm single_run.py
rm postprocess_async.py
rm inp_to_vtu_with_elsets.py
rm preprocess_data.py
rm train_model.py
rm eval_residuals_by_dimension.py
rm preprocess_validate.py
rm damage_validation.py
rm time_history_animation.py
rm render_vtu_animation.py
rm view_damage_animation_interactive.py

# 删除无用脚本
rm generate_animation_only.py
rm analyze_crack_results.py

# 删除旧输出文件
rm whole_from_inp.vtu
rm element_groups.inp

# 删除缓存
rm -rf __pycache__
rm -rf .vs
```

### 阶段3: 验证清理结果

```bash
# 1. 再次运行完整流程,确保没有问题（按需执行）
cd script
python 01_generate_training_data_main.py
python 02_inp_to_vtu_main.py
python 03_preprocess_training_data_main.py
# ... 等

# 2. 检查目录结构
tree script -L 1

# 期望结果（只保留新脚本）:
# script/
# ├── 01_generate_training_data_main.py
# ├── 01_async_postprocess_auxiliary.py
# ├── 01_generate_training_data_output/
# ├── 02_inp_to_vtu_main.py
# ├── 02_inp_to_vtu_output/
# ├── 03_preprocess_training_data_main.py
# ├── 03_preprocess_training_data_output/
# ├── 04_train_model_main.py
# ├── 04_train_model_output/
# ├── 05_determine_thresholds_main.py
# ├── 05_determine_thresholds_output/
# ├── 06_generate_validation_data_main.py
# ├── 06_async_postprocess_auxiliary.py
# ├── 06_single_run_auxiliary.py
# ├── 06_generate_validation_data_output/
# ├── 07_preprocess_validation_data_main.py
# ├── 07_preprocess_validation_data_output/
# ├── 07_README.md
# ├── 08_damage_validation_main.py
# ├── 08_damage_validation_output/
# ├── 08_README.md
# ├── 09_render_time_history_main.py
# ├── 09_render_time_history_output/
# ├── 09_README.md
# ├── 10_render_vtu_animation_main.py
# ├── 10_interactive_vtu_viewer_helper.py
# ├── 10_render_vtu_animation_output/
# ├── 10_README.md
# ├── camera_position.json
# ├── FEM_model/
# └── 重构完成总结.md
```

---

## 📊 清理效果统计

### 删除文件数量

| 类型 | 数量 | 文件大小估计 |
|------|------|--------------|
| 主控脚本 | 1 | ~10 KB |
| 旧配置文件 | 2 | ~5 KB |
| 旧工具模块 | 4个文件（含目录） | ~10 KB |
| 旧流程脚本 | 13 | ~150 KB |
| 无用脚本 | 2 | ~20 KB |
| 旧输出文件 | 2 | ~500 KB |
| 缓存目录 | 2 | ~100 KB |
| **总计** | **约26项** | **~795 KB** |

### 清理前后对比

```
清理前:
script/
├── 60个Python文件（混杂新旧脚本）
├── 配置分散（config.py + 各脚本顶部）
├── 工具模块（utils/）
├── 输出文件混乱（script/ + _output/）
└── 大量缓存和临时文件

清理后:
script/
├── 15个Python文件（10个主流程 + 3个辅助 + 1个分支工具 + 1个汇总文档）
├── 配置集中（各脚本顶部）
├── 无外部依赖（自包含）
├── 输出规范（仅 _output/ 目录）
└── 清爽整洁
```

---

## 🎯 清理后的最终结构

```
c:\data\AE-main\
├── whole.inp                                  # FEM模型输入文件
├── 工作区重组原则.md
├── 脚本重组方案.md
├── 损伤可疑度推导.md
├── VTU渲染使用说明.md
├── 清理方案.md                                # 本文档
├── 美工/
├── 图片存档/
├── elsarticle/
└── script/
    ├── 01_generate_training_data_main.py     # 流程01: 生成训练数据
    ├── 01_async_postprocess_auxiliary.py     # 流程01辅助: 异步后处理
    ├── 01_generate_training_data_output/
    │
    ├── 02_inp_to_vtu_main.py                 # 流程02: INP转VTU
    ├── 02_inp_to_vtu_output/
    │   ├── whole_from_inp.vtu
    │   ├── measures_ID_auto.csv              # 重要: 后续流程依赖
    │   └── element_sets_summary.txt
    │
    ├── 03_preprocess_training_data_main.py   # 流程03: 训练数据预处理
    ├── 03_preprocess_training_data_output/
    │
    ├── 04_train_model_main.py                # 流程04: 模型训练
    ├── 04_train_model_output/
    │
    ├── 05_determine_thresholds_main.py       # 流程05: 确定阈值
    ├── 05_determine_thresholds_output/
    │
    ├── 06_generate_validation_data_main.py   # 流程06: 生成验证数据
    ├── 06_async_postprocess_auxiliary.py     # 流程06辅助: 异步后处理
    ├── 06_single_run_auxiliary.py            # 流程06辅助: 单次运行
    ├── 06_generate_validation_data_output/
    │
    ├── 07_preprocess_validation_data_main.py # 流程07: 验证数据预处理
    ├── 07_preprocess_validation_data_output/
    ├── 07_README.md
    │
    ├── 08_damage_validation_main.py          # 流程08: 损伤识别验证
    ├── 08_damage_validation_output/
    ├── 08_README.md
    │
    ├── 09_render_time_history_main.py        # 流程09: 时历动画渲染
    ├── 09_render_time_history_output/
    ├── 09_README.md
    │
    ├── 10_render_vtu_animation_main.py       # 流程10: VTU 3D动画渲染
    ├── 10_interactive_vtu_viewer_helper.py   # 流程10分支工具: 交互式查看器
    ├── 10_render_vtu_animation_output/
    ├── 10_README.md
    │
    ├── camera_position.json                  # 相机位置配置
    ├── FEM_model/                            # FEM模型文件
    └── 重构完成总结.md
```

---

## ✅ 清理确认清单

完成清理后,请确认以下几点:

- [ ] 所有新流程脚本（01-10）运行正常
- [ ] `main.py` 已删除或备份（不再需要）
- [ ] 旧配置文件（`config.py`, `config_runtime.py`）已删除或备份
- [ ] 旧工具模块（`utils/`）已删除或备份
- [ ] 旧流程脚本（13个）已删除或备份
- [ ] 无用脚本（2个）已删除或备份
- [ ] 旧输出文件已移到 `_output/` 目录或删除
- [ ] Python缓存目录已清理
- [ ] `script/` 目录结构清晰,仅保留新脚本和输出目录
- [ ] 再次运行完整流程,确认无错误

---

## 💡 额外建议

### 1. 版本控制（如果使用Git）

```bash
# 清理前创建标签
git tag -a "v1.0-pre-cleanup" -m "重构完成,清理前版本"
git push origin v1.0-pre-cleanup

# 执行清理（使用git rm）
git rm script/config.py
git rm script/config_runtime.py
git rm -r script/utils
git rm script/batch_controller.py
# ... (其他文件)

# 提交清理
git commit -m "清理: 删除旧配置、工具模块和重构前的脚本"

# 清理后创建标签
git tag -a "v1.0-post-cleanup" -m "重构完成,清理后版本"
git push origin v1.0-post-cleanup
```

### 2. 文档归档

建议将以下文档也移到 `archive/` 或删除（如果不再需要）:
- `工作区重组原则.md` → 保留（作为设计文档）
- `脚本重组方案.md` → 保留（作为设计文档）
- `INP转VTU工作流程说明-新方法.md` → 可归档
- `000-一次性-odb2vtuCLI命令.txt` → 可归档

### 3. 数据备份

清理前建议备份重要数据:
```bash
# 备份输出目录（如果有重要结果）
tar -czf backup_outputs_$(date +%Y%m%d).tar.gz script/*_output/

# 备份外部数据（如果需要）
tar -czf backup_abaqus_data_$(date +%Y%m%d).tar.gz C:/abaqus_gen_data/
```

---

## 🚀 清理后的优势

1. ✅ **目录结构清晰**: 一目了然的执行流程
2. ✅ **无冗余代码**: 删除所有过时和重复的代码
3. ✅ **易于维护**: 每个脚本自包含,无交叉依赖
4. ✅ **参数集中**: 所有参数在脚本顶部,易于调整
5. ✅ **文档完整**: 每个流程都有对应的README
6. ✅ **统一规范**: 所有脚本遵循相同的命名和架构
7. ✅ **便于协作**: 新成员可以快速理解项目结构

---

**清理负责人**: GitHub Copilot  
**文档日期**: 2025-11-02  
**状态**: ✅ 待执行（建议按阶段逐步清理）  

